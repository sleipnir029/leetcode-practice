# Patterns — trigger → approach

The point of this file is the **Trigger** column. In an interview you go from
problem statement → pattern, so that's the direction this index is built in.
Memorizing 75 solutions transfers nothing; recognizing 15 triggers transfers everything.

One row per debrief. Reread the Trigger column before each session — it takes 60 seconds.

| Trigger phrase in the problem | Pattern | Data structure | Complexity | Problems |
|---|---|---|---|---|
| _(first row lands after problem 1)_ | | | | |
